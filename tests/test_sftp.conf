[global]
cluster = testcluster
environment = production
version = latest
destination = /tmp/test_hpc-config
areas = default
mode = sftp

[sftp]
hosts = localhost
username = testuser
private_key = ./ssh_testkey

